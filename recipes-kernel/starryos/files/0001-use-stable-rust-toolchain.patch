From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Yocto Build <yocto@build.local>
Date: Thu, 2 Jan 2026 00:00:00 +0000
Subject: [PATCH] Use stable Rust toolchain instead of nightly

Yocto's rust-native provides stable Rust. Change toolchain to stable
to match Yocto's Rust version.

The RUSTC_BOOTSTRAP=1 environment variable is set during build to
allow using unstable features with stable Rust.

Signed-off-by: Yocto Build <yocto@build.local>
---
 rust-toolchain.toml | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)

diff --git a/rust-toolchain.toml b/rust-toolchain.toml
index 1234567..abcdefg 100644
--- a/rust-toolchain.toml
+++ b/rust-toolchain.toml
@@ -1,6 +1,6 @@
 [toolchain]
 profile = "minimal"
-channel = "nightly-2025-12-12"
+channel = "stable"
 components = ["rust-src", "llvm-tools", "rustfmt", "clippy"]
 targets = [
     "x86_64-unknown-none",
